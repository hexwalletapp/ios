// SampleStakes.swift
// Copyright (c) 2021 Joe Blau

import Foundation

#if DEBUG
//    private let stakeId = try! ABIEncoder.encode(BigUInt(100))
//    private let stakedHearts = try! ABIEncoder.encode(BigUInt(100))
//    private let stakeShares = try! ABIEncoder.encode(BigUInt(100))
//    private let lockedDay = try! ABIEncoder.encode(UInt16(100))
//    private let stakedDays = try! ABIEncoder.encode(UInt16(100))
//    private let unlockedDay = try! ABIEncoder.encode(UInt16(100))
//    private let isAutoStake = try! ABIEncoder.encode(Bool(true))
//
//    private let data = stakeId.hexString + stakedHearts.hexString + stakeShares.hexString + lockedDay.hexString + stakedDays.hexString + unlockedDay.hexString + isAutoStake.hexString
//    private let values = try! ABIDecoder.decodeData(data, types: [BigUInt.self, BigUInt.self, BigUInt.self, UInt16.self, UInt16.self, UInt16.self, Bool.self], asArray: false)
//
//    let sampleStake = try! Stake(values: values)!

#endif
